open vim buffergator
,b

search & replace through all file
:%s/search/replace/g

open file under cursor
gf (in normal mode)

attach runner to session/pane
:VA

toggle tagbar
,tb

toggle NERDTree
,k

send to runner (if selected text, sends lines, otherwise sends file)
,l
